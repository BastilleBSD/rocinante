ARG RELEASE
ARG TEMPLATE

CMD bastille bootstrap ${RELEASE}

INCLUDE core/destroy-release --arg OPTIONS="-f" --arg RELEASE=${RELEASE}

CMD bastille bootstrap ${RELEASE} update

INCLUDE core/destroy-release --arg RELEASE=${RELEASE}

CMD bastille bootstrap ${TEMPLATE}