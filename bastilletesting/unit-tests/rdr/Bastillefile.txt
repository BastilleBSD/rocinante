# unit-tests/rdr

ARG JAIL
ARG ACTION
ARG PROTOCOL
ARG HOST_PORT
ARG JAIL_PORT
ARG LOG
ARG RELEASE
ARG IP
ARG INTERFACE

INCLUDE core/create --arg JAIL=${JAIL} --arg RELEASE=${RELEASE} --arg IP=${IP} --arg INTERFACE=${INTERFACE}

INCLUDE core/rdr --arg JAIL=${JAIL} --arg ACTION=${ACTION} --arg PROTOCOL=${PROTOCOL} --arg HOST_PORT=${HOST_PORT} --arg JAIL_PORT=${JAIL_PORT} --arg LOG=${LOG}

INCLUDE core/destroy-jail --arg OPTIONS="-afxy" --arg JAIL=${JAIL}