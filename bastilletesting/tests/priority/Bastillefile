# tests/priority

ARG OPTIONS="--priority 80"
ARG JAIL
ARG RELEASE
ARG IP
ARG INTERFACE

INCLUDE unit-tests/create-priority --arg OPTIONS=${OPTIONS} --arg JAIL=${JAIL} --arg RELEASE=${RELEASE} --arg IP=${IP} --arg INTERFACE=${INTERFACE}