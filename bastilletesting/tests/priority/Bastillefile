# tests/priority

ARG OPTIONS="--priority 
ARG ARGS="80"
ARG JAIL=attica
ARG RELEASE=14.2-RELEASE
ARG IP=10.1.1.2
ARG INTERFACE=vtnet0

INCLUDE unit-tests/create-priority --arg OPTIONS=${OPTIONS} --arg ARGS=${ARGS} --arg JAIL=${JAIL} --arg RELEASE=${RELEASE} --arg IP=${IP} --arg INTERFACE=${INTERFACE}